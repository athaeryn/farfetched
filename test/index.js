var assert = require("assert"),
    farfetched = require("../lib");


describe("farfetched", function() {
  it("should create handlers");
});

describe("farfetched.clear", function() {
  it("should clear the handler");
});
